#include<stdio.h>
int main()
{
int n,i,c=0,a[50];
printf("enter no.of elements");
scanf("%d",&n);
printf("enter elements");
for(i=0;i<n;i++)
scanf("%d",&a[i]);
for(i=0;i<n;i++)
{
    if(a[i]>0)
    a[c++]=a[i];
}
while(c<n)
{
a[c++]=0;
}
for(i=0;i<n;i++)
{
    printf(" % d",a[i]);
}
}
